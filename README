directory structure:

/lib:
  This directory contains the concurrent effect handler code

/src:
  This directory contains the sample application. Which is a simple demos a GUI applications that stays responsive when thread delays are used to showcase that multiple threads are used.

/test:
 contains test that mainly serve to check whether the desired semantics of the library is achieved.
