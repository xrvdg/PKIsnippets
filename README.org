directory structure:
\src 
  Collection of snippets for a effects system that supports concurrency, graph drawing, ThreepennyGUI event handling.

\ops
  nix snippets to build a container containing neo4j and this project.

for completeness the sigma.js code is added as submodule.
Drawing code currently assumes that sigma.js is build in de directory ./sigma.js
